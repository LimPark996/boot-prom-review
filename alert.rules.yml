groups:
  - name: example
    rules:
      - alert: HighRequestRate
        expr: rate(http_server_requests_seconds_count[1m]) > 10
        for: 1m
        labels:
          severity: warning
        annotations:
          summary: "Request rate high"